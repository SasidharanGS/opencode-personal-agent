{
  "name": "opencode-personal-agent",
  "version": "0.1.0",
  "description": "Background reflection, /wrap, /promote, and /agents-edit slash commands for opencode.",
  "type": "module",
  "main": "./dist/plugin.js",
  "exports": {
    ".": "./dist/plugin.js"
  },
  "files": [
    "dist",
    "skills",
    "commands",
    "scripts/install-extras.sh",
    "README.md",
    "ARCHITECTURE.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SasidharanGS/opencode-personal-agent.git"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build src/plugin.ts --outdir dist --format esm --target node",
    "prepare": "bun build src/plugin.ts --outdir dist --format esm --target node",
    "install-extras": "bash scripts/install-extras.sh"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
